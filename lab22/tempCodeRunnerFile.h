, T e) : start(s), end(e) {
        //     if (e < s) {
                
        //         // throw std::invalid_argument("End must not be less than start.");
        //     }
        // }

        // bool operator<(const Interval& other) const {
        //     return end < other.start;
        // }